<?php
// Check email and password
/*$req = $bdd->prepare('SELECT id, email, send_condition, frequency, link_limit, selected_days, selected_hour, mail_design, language, background_result FROM users_table_serge WHERE users = :pseudo AND password = :pass');
$req->execute(array(
	'pseudo' => $pseudo,
	'pass' => $password));
	$result = $req->fetch();
	$req->closeCursor();*/
?>
